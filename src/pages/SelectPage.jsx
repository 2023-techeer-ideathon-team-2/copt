import ChooseQuestion from "../components/ChooseQuestion";

const SelectPage = () => {
  return (
    <div
      style={{
        display: 'flex',
        justifyContent: 'center',
        alignItems: 'center',
        height: '100%',
      }}>
        <ChooseQuestion/>
    </div>
  );
};

export default SelectPage;
